import { otherText } from "../../constants";

const ShopOrders = () => {
	return (
		<div className="w-full h-full flex items-center justify-center">
			<h1>{otherText.shopOrdersTitle}</h1>
		</div>
	);
};

export default ShopOrders;
